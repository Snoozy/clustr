# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /ping                       controllers.Application.ping
GET     /get_map                    controllers.Application.getMap